# PostgreSQL password file
# For production, generate a secure password:
#   openssl rand -base64 32
# Then use: echo "your_secure_password" > postgres_password.txt
POSTGRES_PASSWORD=your_secure_password_here
